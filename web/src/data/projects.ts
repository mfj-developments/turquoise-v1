import type { Project } from "@/types/project";

/**
 * Active project cards for the site.
 *
 * Keep this list empty while real client work is in progress.
 * When you are ready to showcase entries again, copy objects from
 * `projects-archive.json` or generate new ones that satisfy the
 * `Project` type and export them in this array.
 */
export const projects: Project[] = [];
